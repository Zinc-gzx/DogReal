# ✅ 宠物档案功能 - 测试指南

## 🎉 构建成功！应用已启动

### 当前运行状态

**✅ Flutter应用**
- 运行在: iPhone 16 Pro 模拟器
- 调试服务: http://127.0.0.1:55942/
- 状态: 正在运行

**✅ 后端API服务器**
- 运行在: http://localhost:3000
- MongoDB: 已连接
- 状态: 正在运行

---

## 📋 测试流程

### 1. 新用户注册并创建宠物档案

#### 步骤 1: 注册新用户
1. 在模拟器中，点击「立即注册」
2. 填写信息：
   - 用户名: `测试用户1`
   - 邮箱: `test1@dogreal.com`
   - 密码: `123456`
   - 确认密码: `123456`
3. 勾选「同意服务条款和隐私政策」
4. 点击「注册」按钮
5. **预期结果**: 注册成功后自动跳转到「创建宠物档案」页面

#### 步骤 2: 创建宠物档案
1. 点击头像圆圈，选择：
   - 「拍照」或「从相册选择」上传宠物照片
2. 填写宠物信息：
   - **宠物名字**: `旺财` (必填)
   - **品种**: 点击选择，如「金毛寻回犬」(必填)
   - **生日**: 点击选择日期，如 `2022-05-15` (必填)
   - **性别**: 选择「男孩」「女孩」或「未知」
   - **体重**: `25.5` (可选)
   - **个人简介**: `一只可爱活泼的金毛` (可选)
3. 点击「创建档案」按钮
4. **预期结果**: 显示加载动画 → 创建成功 → 跳转到主页

---

### 2. 测试表单验证

#### 必填字段验证
- ❌ 不填「宠物名字」→ 显示错误提示
- ❌ 不选择「品种」→ 显示错误提示
- ❌ 不选择「生日」→ 显示Toast提示
- ✅ 填写完整 → 可以提交

#### 字符限制验证
- 宠物名字不能超过50个字符
- 个人简介不能超过500个字符

---

### 3. 测试图片上传

#### 头像选择器功能
1. 点击头像圆圈
2. 弹出选项：
   - 📷 拍照
   - 🖼️ 从相册选择
   - 🗑️ 移除照片 (已有照片时显示)

#### 测试场景
- ✅ 选择相册图片 → 头像预览显示
- ✅ 拍照 → 头像预览显示
- ✅ 移除照片 → 恢复默认占位符

---

### 4. 测试品种选择

1. 点击「品种」输入框
2. 弹出品种选择列表：
   - 金毛寻回犬
   - 拉布拉多
   - 哈士奇
   - 柴犬
   - 柯基
   - 泰迪
   - 边境牧羊犬
   - 萨摩耶
   - 博美
   - 比熊
   - 其他
3. 点击任一品种
4. **预期结果**: 选中品种显示在输入框中

---

### 5. 测试日期选择

1. 点击「选择生日」
2. 打开日期选择器
3. 选择任意日期
4. **预期结果**: 
   - 日期显示格式: `2022-05-15`
   - 颜色从灰色变为白色

---

### 6. 测试性别选择

1. 查看三个性别按钮：
   - 👨 男孩
   - 👩 女孩
   - ❓ 未知
2. 点击任一按钮
3. **预期结果**: 
   - 选中按钮背景变白色，文字变黑色
   - 未选中按钮保持灰色边框

---

### 7. 测试API集成

#### 使用Postman测试API (可选)

```bash
# 1. 注册用户
POST http://localhost:3000/api/auth/register
Content-Type: application/json

{
  "username": "test2",
  "email": "test2@dogreal.com",
  "password": "123456"
}

# 2. 复制返回的 token

# 3. 创建宠物档案
POST http://localhost:3000/api/pets
Authorization: Bearer YOUR_TOKEN_HERE
Content-Type: application/json

{
  "name": "旺财",
  "breed": "金毛寻回犬",
  "birthday": "2022-05-15",
  "gender": "male",
  "bio": "一只可爱的金毛",
  "weight": 25.5
}

# 4. 获取我的宠物
GET http://localhost:3000/api/pets/my-pet
Authorization: Bearer YOUR_TOKEN_HERE
```

---

## 🐛 可能遇到的问题

### 问题 1: 无法连接后端API
**症状**: 点击「创建档案」后显示网络错误

**解决方案**:
1. 检查后端服务器是否运行: 访问 http://localhost:3000/health
2. 如果在真机测试，需要修改 `api_config.dart`:
   ```dart
   // 将 localhost 改为你的Mac局域网IP
   static const String baseUrl = 'http://192.168.1.100:3000/api';
   ```
3. 获取Mac IP地址:
   ```bash
   ifconfig | grep "inet " | grep -v 127.0.0.1
   ```

### 问题 2: 图片上传失败
**症状**: 选择图片后显示上传失败

**解决方案**:
1. 检查 `backend/uploads/avatars` 目录是否存在
2. 检查文件权限
3. 查看后端日志的错误信息

### 问题 3: 热重载后页面白屏
**症状**: 修改代码保存后，应用显示空白

**解决方案**:
- 按 `R` 进行热重启（完全重启应用）
- 或重新运行 `flutter run`

### 问题 4: iOS相机权限问题
**症状**: 点击拍照没有反应

**解决方案**:
在 `ios/Runner/Info.plist` 中添加:
```xml
<key>NSCameraUsageDescription</key>
<string>需要访问相机来拍摄宠物照片</string>
<key>NSPhotoLibraryUsageDescription</key>
<string>需要访问相册来选择宠物照片</string>
```

---

## 📸 UI效果预览

### 宠物档案创建页面特点
- 🎨 黑色背景（BeReal风格）
- ⚪ 圆形头像选择器
- 📝 简洁的表单设计
- 🎯 清晰的必填标识
- 💫 流畅的交互动画
- ✨ 白色主题按钮

---

## ⌨️ 开发快捷键

在终端中（Flutter运行时）：
- `r` - 热重载（保留状态更新UI）
- `R` - 热重启（完全重启应用）
- `q` - 退出应用
- `d` - 分离（应用继续运行，退出flutter run）

---

## 🎯 测试检查清单

- [ ] 注册新用户成功
- [ ] 自动跳转到宠物档案创建页
- [ ] 头像选择器工作正常
- [ ] 相机拍照功能正常
- [ ] 相册选择功能正常
- [ ] 宠物名字输入验证
- [ ] 品种选择器正常
- [ ] 生日选择器正常
- [ ] 性别按钮切换正常
- [ ] 可选字段（体重、简介）可以留空
- [ ] 提交按钮显示加载状态
- [ ] 创建成功跳转到主页
- [ ] 表单验证错误提示正确
- [ ] 网络错误提示正确

---

## 📊 数据库验证

### 检查MongoDB中的数据

1. 打开MongoDB Compass或Atlas
2. 连接到数据库
3. 查看 `users` 集合：
   ```javascript
   { username: "test1", email: "test1@dogreal.com" }
   ```
4. 查看 `pets` 集合：
   ```javascript
   {
     name: "旺财",
     breed: "金毛寻回犬",
     birthday: ISODate("2022-05-15"),
     gender: "male",
     owner: ObjectId("..."),
     avatar: "http://localhost:3000/uploads/avatars/avatar_xxx.jpg"
   }
   ```

---

## 🚀 下一步开发

宠物档案创建功能已完成！可以继续开发：

### 第2阶段：Feed页面
- [ ] 创建Post模型（帖子）
- [ ] 实现Feed API
- [ ] Feed页面UI
- [ ] 今日发布锁定功能

### 第3阶段：相机功能
- [ ] 相机页面
- [ ] 前后摄像头切换
- [ ] 拍照并发布
- [ ] 2分钟倒计时

### 第4阶段：好友系统
- [ ] 好友模型
- [ ] 添加好友
- [ ] 好友列表
- [ ] 搜索用户

---

## 💡 开发提示

1. **查看实时日志**:
   - 后端日志：查看运行后端的终端
   - Flutter日志：查看运行Flutter的终端

2. **调试网络请求**:
   ```dart
   print('API Request: ${response.statusCode}');
   print('Response: ${response.body}');
   ```

3. **清理缓存**:
   ```bash
   cd dogreal_app
   flutter clean
   flutter pub get
   ```

---

## ✨ 恭喜！

你已经成功完成了宠物档案创建功能的开发和测试！

开始测试吧！ 🐾
